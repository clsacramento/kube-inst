---
# Configures k8s nodes

 - hosts: kube
   roles: 
   - role: kube-install
 - hosts: master
   roles:
    - role: master
 - hosts: kube
   roles:
    - role: flannel
 - hosts: kubelets
   roles:
    - role: kubelets 
