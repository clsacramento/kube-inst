---
# Sets up yum proxy and run yum update
# NB: I didnt test 'cause did this part manually
 - hosts: kube
   roles:
   - role: preconf
   - role: ntp
   - role: docker
 - hosts: all
   roles:
   - role: etc-hosts
